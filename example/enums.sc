enum Color {
    Red,
    Green,
    Blue,
}

enum Option {
    Some(i32),
    None,
}

enum Message {
    Quit,
    Move { x: i32, y: i32 },
    Write(str),
}

fn main() {
    let x = 5;
    return 0;
}
